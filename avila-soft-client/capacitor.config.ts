import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fidev.avsoftapp',
  appName: 'AvilaSoft',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
